# Replit Scalper — Текущее состояние системы (2026-09-08)

> Этот документ описывает систему **на текущий момент**: как реально устроен бот,
> какие пресеты включены, как генерируются сигналы, как считаются стопы/тейки,
> и как интерпретировать статистику. Написан для того, чтобы его можно было
> анализировать независимо (в т.ч. на стороне), не читая весь код.

---

## 0. TL;DR — что это

Автоматический **бумажный (paper) торговый бот** для Binance Futures (USDT-M),
который на каждом из 35 символов (по одному процессу на монету) слушает **1-часовые
свечи**, на закрытии свечи опрашивает **46 индикаторных пресетов**, выбирает один
сработавший сигнал с **наибольшим объёмом** и открывает **одну позицию** с
**ATR-стопами** (SL = 1.5×ATR, TP = RR×SL). Позиция держится до SL или TP1
(`tp1_close_pct: 100` — закрывается целиком, TP2 не используется).

Сейчас это **paper-торговля**: реальные ордера не выставляются, все сделки
симулируются с депозита `paper_balance: 1000 USDT` на каждый символ.
Цель текущей фазы — **найти выигрышные закономерности** (по пресетам, направлениям,
часам), а не заработать.

**Режим:** `paper`. **Таймфрейм:** `1h`. **HTF-фильтры:** выключены.
**LLM-фильтр:** выключен. **Recovery:** выключен.

> ⚠️ **С 2026-09-08 в системе активен ОДИН сигнал — `inside_bar_rsi_long`**
> (Inside Bar пробой LONG + RSI≥70). Все 46 пресетов закомментированы. Это
> paper-forward тест правила, найденного в анализе (см. раздел 7.8). Подробности и
> критерий отключения — там же.

**Финальный вывод серии экспериментов (сент 2026):** несмотря на ~20 проверенных
конфигураций (consensus, ADX-фильтры, режимная адаптация, блок часов, отключение
пресетов, гибридный вход, разные RR/ТФ), **ни одна не дала устойчивый плюс на
независимых (out-of-sample) данных** (март–май 2026). Лучшая in-sample конфигурация
(consensus=2 + ADX≤25 + RR 2:1 на 1h, ≈ −65 за июн–сен) даёт −147 на марте–мае.
Подобранные «COMBO-правила» (часы/пресеты) — подгонка под один период. Найден и
исправлен баг TP1-full-close в бэктестере. Детали — в разделе 7.

---

## 1. Архитектура и компоненты

```
replit_scalper/
├── bot/
│   ├── main.py                 # Процесс бота: цикл свечей, on_candle, открытие/закрытие
│   ├── config.py               # Dataclass Config + загрузка YAML
│   ├── strategy.py             # Индикаторы + 46 пресетов (get_signal / get_all_signals)
│   ├── preset_config.py        # PRESET_CONFIG: tp/sl/лимиты по пресетам
│   ├── market_data.py          # REST-поллинг свечей (каждые 10с), прогрева
│   ├── backtester.py           # Движок бэктеста (run_backtest_on_df)
│   ├── position_tracker.py     # Трекер позиции: хиты SL/TP, состояние
│   ├── order_manager.py        # Расчёт qty (в paper — симуляция входа)
│   ├── db_reporter.py          # HTTP-репортёр в API (trades, heartbeat, llm_status)
│   ├── config_<SYMBOL>.yaml    # 35 per-symbol конфигов (по одному на монету)
│   ├── config.yaml             # Общий шаблон (НЕ используется ботами напрямую)
│   ├── preset_config.py
│   └── *.py (сервисные)        # logger, notifier, rate_limit, recovery_client...
├── artifacts/
│   ├── api-server/             # Express API :5000 (SQLite bot.db)
│   └── dashboard/              # React/Vite дашборд :5173
├── data/
│   └── bot.db                  # SQLite: таблицы bots, trades, recovery_chains
└── README.md                   # Общая (местами устаревшая) документация
```

**Схема данных:**
- `bots` — по строке на символ: режим, таймфрейм, is_running, heartbeat, position, llm_status.
- `trades` — каждая сделка: symbol, direction (LONG/SHORT), preset, entry/exit,
  pnl, exit_reason (SL/TP1/TP2), status (`open`/`closed`/`rejected`), is_open, commission.
- API: `GET /api/bots`, `GET /api/trades`, `POST /api/trading/close-and-reset` и т.д.

---

## 2. Конфигурация на текущий момент

### 2.1 Общие параметры (одинаковы во всех конфигах)

| Параметр | Значение | Смысл |
|---|---|---|
| `mode` | `paper` | Симуляция, реальных ордеров нет |
| `timeframe` | `1h` | Торговля по 1-часовым свечам |
| `htf_enabled` / `htf2_enabled` | `false` | HTF-фильтры выключены |
| `use_fixed_tp_sl` | `false` | Используется ATR-расчёт стопов (не фикс. %) |
| `atr_tp_multiplier` | (не задан → 2.0) | RR: TP = множитель × SL |
| `llm_enabled` | `false` | LLM-фильтр сигналов выключен |
| `auto_mode` | `true` | Сигналы подтверждаются автоматически |
| `signal_cooldown_min` | `0` | Кулдауна между сигналами нет |
| `max_open_per_cycle` | `0` | Лимита сделок за цикл нет |
| `tp1_close_pct` | `100` | TP1 закрывает позицию **целиком** (TP2 не используется) |
| `sl_pct` / `tp1_pct` / `tp2_pct` | 0.5 / 1.0 / 1.0 | Fallback %, если ATR недоступен |
| `risk_pct` | 3.0 | % баланса в риск на сделку |
| `fixed_risk_usd` | 0.5 | **Активный** режим расчёта qty: фикс. риск 0.5 USDT |
| `paper_balance` | 1000.0 | Стартовый депозит на символ |
| `commission_pct` | 0.05 | Комиссия taker, вычитается из PnL |
| `adx_threshold` | 20.0 | ADX-фильтр (пресеты ADX требуют adx ≥ 20) |

### 2.2 Параметры, различающиеся по монетам

Каждый символ имеет **свой** `config_<symbol>.yaml` с индивидуальными значениями:

| Параметр | Диапазон | Пример (BTC) |
|---|---|---|
| `leverage` | 50–150 | 150 |
| `volume_multiplier` | 1.0–2.2 | 1.8 |
| `ema_fast` / `ema_slow` | 5–18 / 8–52 | 12 / 51 |

**Важно:** эти параметры заданы **индивидуально под каждую монету** (результат ранней
оптимизации) и **различаются между монетами**. При бэктесте нужно использовать
per-symbol конфиг, а не общий шаблон.

### 2.3 Индикаторные параметры (общие)

`rsi_period 14 / rsi_low 30 / rsi_high 70`, `macd 12/26/9`,
`bb_period 20 / bb_std 2.0`, `volume_ma_period 20`, `adx_threshold 20.0`.

---

## 3. Пресеты — полный список (все 46 включены)

> **Важно понять устройство:** каждый пресет — это **функция-сигнал** в `strategy.py`
> (например `get_signal_macd_hist_trend`). Пара `_long`/`_short` — это **одна и та же
> функция**, вызванная из `get_all_signals` дважды (под разными именами). Сама функция
> **внутри решает**, LONG или SHORT сигнал выдать — суффикс в имени НЕ ограничивает
> направление. Поэтому в базе можно увидеть сделку `preset=macd_hist_trend_long`
> с `direction=SHORT`.

### 3.1 Стратегии и условия входа (логика пресетов)

| # | Пресет | Индикатор | Условие LONG / SHORT |
|---|---|---|---|
| 1 | `ema_cross` | EMA fast/slow | Пересечение EMA вверх/вниз + объём ≥ volume_ma×mult |
| 2 | `sma_cross` | SMA fast/slow | Аналог EMA, но SMA |
| 3 | `rsi_bounce` | RSI | RSI был <30 (LONG) / >70 (SHORT), затем разворот |
| 4 | `rsi_divergence` | RSI + цена | Цена на минимуме, RSI выше чем на прошлом минимуме (LONG), и наоборот |
| 5 | `macd_momentum` | MACD | Пересечение MACD и сигнальной |
| 6 | `macd_hist_trend` | Гистограмма MACD | hist растёт 3 свечи подряд >0 (LONG), падает <0 (SHORT) |
| 7 | `bb_bounce` | Bollinger | Возврат цены внутрь после касания нижней/верхней полосы |
| 8 | `bb_squeeze_breakout` | Bollinger | Сжатие ширины 3 свечи + пробой полосы |
| 9 | `stoch_bounce` | Stochastic | K <20 → рост (LONG), K >80 → падение (SHORT) |
| 10 | `stoch_obos` | Stochastic | Пересечение K уровней 20/80 |
| 11 | `vwap_return` | VWAP | Цена пересекла VWAP снизу/сверху |
| 12 | `vwap_band` | VWAP | Значимое отклонение от VWAP (>1%) + возврат |
| 13 | `atr_breakout` | ATR | Пробой high/low + 0.5×ATR при растущем ATR |
| 14 | `atr_trailing` | ATR | Аналогично breakout (вариант) |
| 15 | `volume_spike` | Volume | Всплеск объёма ≥2×MA + направление цены |
| 16 | `volume_trend` | Volume MA | Рост volume_ma + рост/падение цены |
| 17 | `supertrend` | Supertrend | Пересечение цены и линии Supertrend |
| 18 | `supertrend_reentry` | Supertrend | Re-entry после смены тренда |
| 19 | `adx_trend` | ADX | ADX ≥ threshold + пересечение +DI/−DI |
| 20 | `ichimoku` | Ichimoku | Цена вне облака + Tenkan>Kijun (LONG) |
| 21 | `ichimoku_tk_cross` | Ichimoku | Кросс Tenkan/Kijun внутри облака |
| 22 | `morning_star` | Свечной паттерн | Утренняя звезда (только LONG) |
| 23 | `evening_star` | Свечной паттерн | Вечерняя звезда (только SHORT) |
| 24 | `engulfing` | Свечной паттерн | Бычье/медвежье поглощение |

Всего **46 пресетов** (22 стратегии × long/short + morning_star + evening_star).

### 3.2 TP/SL по пресетам (preset_config.py)

Все пресеты сейчас имеют одинаковые значения:
```python
{"tp": 1.5, "sl": 0.5, "max_per_preset": 0}
```
- `max_per_preset: 0` — нет лимита одновременных позиций на пресет.
- Эти значения используются **только как fallback**, если ATR недоступен (см. §4).
- Есть `mode` override: `rsi_bounce_long` помечен `"mode": "paper"`.

---

## 4. Расчёт стопов и тейков (ATR-based) — КЛЮЧЕВАЯ ЛОГИКА

Так как `use_fixed_tp_sl: false`, стопы/тейки **не берутся из пресета**, а считаются
от ATR на момент сигнала (`strategy.py::_calc_atr_sl_tp`):

```python
atr_pct = ATR_абс / entry × 100        # волатильность как % от цены
SL% = 1.5 × atr_pct                    # стоп = 1.5 волатильности
TP% = RR × SL%                          # RR = atr_tp_multiplier (сейчас 2.0)
```

**RR настраивается параметром `atr_tp_multiplier`** (в config): 2.0 = TP = 2×SL.
Также поддержаны асимметричные `atr_tp_multiplier_long` / `atr_tp_multiplier_short`
(для разных RR по направлениям — тестируется).

**Примеры (по медианному ATR за месяц, 1h):**
| Монета | ATR% | SL% | TP% (RR 2) |
|---|---|---|---|
| BTC | ~0.38 | 0.57 | 1.14 |
| ETH | ~0.52 | 0.78 | 1.56 |
| SOL | ~0.59 | 0.89 | 1.77 |
| ADA | ~1.00 | 1.50 | 3.00 |
| TRX | ~0.18 | 0.27 | 0.54 |

Стопы адаптируются к волатильности каждой монеты и момента времени.

**Расчёт происходит дважды:**
1. В `_make_signal` (strategy.py) — при генерации сигнала, от цены закрытия свечи.
2. В `main.py` после входа — пересчёт от **реальной** цены входа (с учётом проскальзывания).

**Размер позиции:** активен `fixed_risk_usd = 0.5` → риск 0.5 USDT на сделку.
`qty = fixed_risk_usd / (entry × SL%/100)`. Пример для BTC (цена ~105 000, SL ~0.6%):
`qty = 0.5 / (105000 × 0.006) = 0.5 / 630 ≈ 0.00079 BTC` (≈ 0.0008 BTC; нормализуется под LOT_SIZE).
**Плечо (50–150x) влияет только на маржу, не на qty.**

---

## 5. Цикл работы бота (on_candle)

1. REST-поллинг каждые 10 сек; при закрытии 1h-свечи — `on_candle`.
2. Если позиция открыта → проверка хитов SL/TP1 по текущей цене.
3. Если позиции нет → сбор сигналов: `get_all_signals` вызывает **все 46 пресетов**,
   каждый возвращает `Signal` или `None`.
4. **Выбор сигнала:** сортировка по `volume` (объёму свечи) по убыванию, берётся
   первый — сигнал с **наибольшим объёмом**. (НЕ «лучший по score» — именно max volume.)
5. Фильтры: HTF (выкл), cooldown (0), cycle_limit (0), preset_limit (0), loss-streak (активен,
   но после 3+ убытков подряд), ADX (для adx-пресетов), LLM (выкл).
6. Расчёт ATR-стопов, расчёт qty, «открытие» позиции в трекере, запись сделки в БД.
7. Далее на каждой свече трекер проверяет: цена дошла до SL → закрытие (loss),
   до TP1 → закрытие целиком (win). Запись exit_price/pnl/exit_reason в БД.
8. Heartbeat каждую свечу; watchdog масштабируется от таймфрейма (1h → ~2.5 ч).

**Одна позиция за раз на символ:** пока позиция открыта, новые сигналы по этому
символу игнорируются (`guard`).

---

## 6. Статус отклонённых сделок (rejected)

В `trades` есть статус `rejected` с полем `reject_reason`. Причины:
- `llm_reject` — отклонено LLM (сейчас LLM выключен, таких нет).
- `risk:*`, `skip:*` — risk-лимиты / loss-streak / cooldown.
- **rejected-сделки НЕ учитываются в статистике и не приносят убытка депозиту.**
  Они пишутся для наблюдения (например, чтобы оценить качество LLM).

Сейчас в системе rejected-сделок практически нет (LLM выключен, лимиты отключены),
торговля чистая: только `open` → `closed`.

---

## 7. Текущая торговля и что смотрим

> ⚠️ **ВАЖНО ПРО ВАЛИДНОСТЬ ЦИФР:** блоки «Бэктест 3 мес» и «Эксперименты с RR» ниже
> (до раздела 7.3) получены ДО исправления бага TP1-full-close в бэктестере (см. 7.3).
> Их winrate (~26%) занижен и PnL невалиден. Валидные данные — в разделах 7.3–7.6.
> Единственная живая (не бэктестная) статистика — неделя paper ниже.

**35 символов** (из `config_*.yaml`): BTC, ETH, BNB, SOL, XRP, DOGE, ADA, LINK, AVAX,
DOT, TRX, LTC, ATOM, APT, ARB, FIL, HBAR, INJ, KAS, NEAR, OP, POL, RENDER, SUI, TAO,
TIA, TON, UNI, WIF, 1000PEPE, AAVE, BCH, FET, IMX, ONT (+ др.).

**Реальная статистика за неделю paper (30 авг — 7 сен):** *(это живые сделки ботов,
не бэктест — период ДО включения consensus/ADX-фильтров, сигнал брался без consensus)*
- 1390 закрытых сделок, winrate ~30%, суммарный PnL **−261 USDT** (на 35 × 1000 = 35k депозита).
- LONG ≈ 0 (около безубытка), **SHORT дал почти весь убыток** (−261).
- Комиссия (0.05%) суммарно −138 USDT — существенна.

**Бэктест 3 мес (1 июн — 7 сен, реальные конфиги, ATR, RR 2:1):**
- LONG: 2144 сделки, winrate 26.7%, PnL −5399
- SHORT: 2088 сделки, winrate 25.8%, PnL −5719
- Вывод: обе стороны убыточны на длинной дистанции; SHORT не «особенно плох».
  Winrate ~26% ниже порога безубытка для RR 2:1 (нужно >33%).

**Эксперименты с RR (бэктест 3 мес):**
| RR | Winrate | PnL |
|---|---|---|
| 2:1 | 26.3% | −11117 |
| 2.5 | 24.4% | −11160 |
| 3:1 | 21.8% | −20197 |
| LONG 2× / SHORT 3× | 23.3% | −18227 |

Расширение TP **ухудшает** результат (winrate падает быстрее, чем растёт размер win).
**Вывод:** изменение RR 2:1→3:1 — тупик; winrate ~26% при RR 2:1 ниже порога 33%.

### 7.1 Per-preset изоляция (бэктест 3 мес, честный sizing)

Каждый пресет прогонялся **изолированно** по всем символам (35 × 46 × 3 мес).
Выводы:
- **Ни один пресет не даёт winrate > 33%** в одиночку — лучшие ~27-28%
  (`ichimoku_tk_cross`, `bb_bounce`, `vwap_return`), большинство 25-27%.
- Пары `_long`/`_short` в изоляции дают **идентичные** результаты (одна функция),
  причём каждая генерирует и LONG, и SHORT сама — суффикс не ограничивает направление.
- Признак того, что проблема системная (вход на 1h / структура сделки / RR),
  а не в конкретных пресетах.

### 7.2 Режим рынка (ADX на входе) — корреляция подтверждена частично

Каждая сделка в бэктесте помечается состоянием рынка на входе
(`regime_adx`, `regime_atr_pct`, `regime_trend`):

| Режим (ADX) | Сделок | Winrate | PnL | avg/сделку |
|---|---|---|---|---|
| trend (≥25) | 52 232 | 26.2% | −2384 | −0.046 |
| weak (15–25) | 37 342 | 25.7% | −2029 | −0.054 |
| flat (<15) | 7 122 | **27.6%** | **−82** | −0.012 |

- Во **флете** система почти безубыточна и winrate выше → часть сделок в тренде
  «портят» общий результат.
- Но даже лучшие пресеты каждого режима дают winrate 27–31% < 33% (порог RR 2:1) —
  режим-фильтр сам по себе не выводит в плюс.
- В тренде закономерно сливают mean-reversion (`stoch_bounce` −160, `rsi_bounce` −152);
  во флете плюсуют `ema_cross`, `atr_breakout`, `volume_trend` (winrate 30-31%).

### 7.3 ВАЖНО: найден и исправлен баг в бэктестере (TP1 full-close)

**Баг:** при `tp1_close_pct=100` трекер в бэктесте закрывал весь объём на TP1, но НЕ
помечал позицию закрытой (оставалась висеть с `remaining_qty=0` до фиктивного TP2).
Это приводило к:
- заниженному winrate (фиктивные TP2-«сделки» с нулевым pnl разбавляли статистику),
- искажению результатов ВСЕХ ранних прогонов (раздел 7 «Бэктест 3 мес», RR-эксперименты).

**Фикс** (`position_tracker.py::apply_hit`): если TP1 закрыл весь остаток — позиция
закрывается сразу, возвращается exit_reason=TP1.

**После фикса реальный winrate = 34.7% (а не 26%)** при RR 2:1 — выше порога
безубытка 33.3%. Все цифры ниже — ПОСЛЕ фикса и являются валидными.
Прежние прогоны (до фикса, winrate ~26%) использовать нельзя.

### 7.4 Финальная матрица экспериментов (ПОСЛЕ фикса, 1 июн — 7 сен, 35 монет, честный sizing, комиссия учтена)

| # | Сценарий | Сделок | Winrate | PnL |
|---|---|---|---|---|
| 1 | база consensus=1, RR 2:1, 1h | 4 232 | ~26%* | ~−11 117* |
| 2 | consensus=2, RR 2:1, 1h | 3 137 | 34.7% | −167 |
| 3 | consensus=3, RR 2:1, 1h | ~3 919 | ~26%* | −196* |
| 4 | **consensus=2 + ADX≤25, RR 2:1, 1h** | 2 245 | **36.2%** | **−65** |
| 5 | consensus=2 + ADX≤25, RR 2.2, 1h | 2 091 | 33.9% | −83 |
| 6 | consensus=2, TP1=60%+TP2=3×SL, 1h | ~2 373 | ~28%* | −637* |
| 7 | consensus=2, 15m | 14 159 | 34.7% | −1 300 |
| 8 | Гибрид: 1h сигнал + M15 подтверждение, consensus=2 | 4 054 | 26.7% | −563 |
| 9 | Режимный консенсус: flat=1, weak=2, trend=3 + блок SHORT в тренде | 2 991 | 35.9% | −81 |
| 10 | OUT-OF-SAMPLE март–май: база consensus=2 + ADX≤25 | 2 316 | 34.5% | **−147** |
| 11 | OUT-OF-SAMPLE март–май: COMBO (база + block_hours + 8 пресетов) | 2 150 | 34.8% | **−121** |

`*` — прогоны 1,3,6 сделаны ДО фикса TP1 (в разделе оставлены для истории, но их PnL
невалиден; валиден только относительный порядок и качественные выводы).

### 7.5 Выводы по экспериментам (для сторонней оценки)

1. **Лучшая найденная конфигурация: consensus=2 + ADX≤25 (вход только при ADX<25) +
   RR 2:1 на 1h → winrate 36.2%, PnL −65 за 3 месяца** (≈ безубыток с учётом
   комиссии 0.05%). По сравнению с базой без фильтров (−11 117) это −99.4% убытка.
2. **Комиссия — ключевой пожиратель edge.** Winrate 36.2% при RR 2:1 даёт
   матожидание ≈ +0.08R/сделку ДО комиссии; комиссия 0.05% от номинала при
   `fixed_risk_usd=0.5` съедает почти весь этот запас.
3. **Расширение RR вредно**: 2.0→2.2→2.5→3.0 winrate падает быстрее, чем растёт
   размер выигрыша (подтверждено и до, и после фикса).
4. **Меньший ТФ не помогает**: 15m даёт тот же winrate 34.7%, но в 4.5 раза больше
   сделок → в 4.5 раза больше комиссии → PnL −1 300 против −167 на 1h.
5. **Гибрид «1h сигнал + M15 подтверждение» провалился** (winrate 26.7%, PnL −563):
   подтверждение = «цена уже двинулась в сторону» = погоня за ценой. Очевидные
   пробои чаще откатываются. Гипотеза «вход раньше» отвергнута данными.
6. **TP1=60% + TP2-раннер** повышает winrate (за счёт частичных фиксаций), но
   ухудшает PnL (фиксирует лишь 60% прибыли, остаток часто закрывается в breakeven,
   + двойная комиссия на два выхода).
7. **Режимный консенсус (flat=1/weak=2/trend=3 + блок SHORT) НЕ побил простой
   ADX≤25** (−81 vs −65). Блок SHORT в тренде отлично сработал (SHORT стал почти
   безубыточным: −26 при winrate 36.1%), но смягчение флета до consensus=1 добавило
   мусорные входы (LONG вырос с 1 088 до 2 157 и остался в минусе −55). Итог:
   простой фильтр ADX≤25 эффективнее адаптации.
8. **Режим-эффект есть**: во флете (ADX<15) система почти безубыточна, в тренде
   убыточна. Mean-reversion-пресеты (stoch_bounce, rsi_bounce) закономерно сливают
   в сильном тренде. ADX≤25-фильтр убирает худшие трендовые входы.

### 7.5a Анализ пар консенсуса (какие стратегии согласуются лучше всего)

Каждая сделка консенсуса помечается списком базовых стратегий, поддержавших
направление (`voting_bases`). Разбивка по комбинациям (consensus=2, 1h, после фикса):

**Лучшие комбинации (mean-reversion + объём / широкое согласие):**
| Комбинация | Сделок | Winrate | PnL |
|---|---|---|---|
| bb_bounce+stoch_bounce+stoch_obos+volume_trend | 66 | 57.6% | +23.6 |
| atr_trailing+ichimoku+volume_trend | 43 | 55.8% | +20.4 |
| atr_breakout+atr_trailing+ichimoku+macd_hist_trend+volume_spike+volume_trend | 70 | 42.9% | +13.3 |
| bb_bounce+stoch_bounce+stoch_obos | 38 | 52.6% | +10.2 |
| evening_star+ichimoku+morning_star | 14 | 57.1% | +6.6 |

**Худшие комбинации (массовые трендовые дуэты):**
| Комбинация | Сделок | Winrate | PnL |
|---|---|---|---|
| ichimoku+macd_hist_trend+volume_trend | 221 | 33.5% | −16.3 |
| rsi_bounce+stoch_bounce+stoch_obos+volume_trend | 65 | 29.2% | −14.1 |
| macd_hist_trend+volume_trend | 62 | 30.6% | −9.1 |
| ichimoku+macd_hist_trend | 271 | 36.5% | −5.7 |

**Выводы по парам:**
- **`ichimoku + macd_hist_trend` (± volume_trend/volume_spike) — главный массовый
  источник слабых сигналов**: 492+221 сделок, стабильный минус. Именно эти две
  стратегии чаще всего согласуются вместе и тянут общий результат вниз. При
  сокращении набора пресетов их убирать в первую очередь.
- **Прибыльны согласия групп mean-reversion + объём** (bb/stoch/rsi_bounce +
  volume_trend) — winrate 43–58%.
- **Свечные утренняя/вечерняя звезда** дают высокий winrate (57–69%), но мало сделок.
- «Согласие многих» ≠ лучше: важно КАКИЕ стратегии согласовались, а не просто их
  число. Широкое согласие 5-6 стратегий работает только когда в нём нет
  доминирующего слабого ядра (ichimoku+macd_hist_trend).

**Предостережение:** правила «чёрных списков комбинаций» подогнаны под тот же период
(риск переобучения) — требуют out-of-sample проверки перед применением.

### 7.6 Out-of-sample проверка (март–май 2026) — ГЛАВНЫЙ ВЫВОД

Для проверки «найденного решения» (COMBO: consensus=2 + ADX≤25 + блок часов
[2,6,10,12,15,16] + исключение 8 пресетов) тот же сценарий прогнан на **другом
периоде — март–май 2026**, который НЕ участвовал в подборе:

| Сценарий | LONG | SHORT | ИТОГО | Winrate |
|---|---|---|---|---|
| База (consensus=2 + ADX≤25), март–май | −13 | −134 | **−147** | 34.5% |
| COMBO (база + часы + 8 пресетов), март–май | +23 | −143 | **−121** | 34.8% |
| База (consensus=2 + ADX≤25), июн–сен (in-sample) | −62 | −3 | **−65** | 36.2% |

**Результаты out-of-sample:**
1. **COMBO НЕ подтвердился.** Заявленный «+$68» на июне–сентябре не воспроизвёлся:
   на марте–мае COMBO даёт **−121**, а не плюс. «Исключить плохие часы + плохие
   пресеты», подобранные на одном периоде, — **подгонка под историю**.
2. COMBO лишь немного улучшает базу out-of-sample (−147 → −121) и только за счёт
   LONG (+23); SHORT остаётся глубоко убыточным (−143) в обоих случаях.
3. **Даже базовая конфигурация нестабильна между периодами**: июн–сен ≈ −65
   (≈безубыток), март–май = −147. Квартальные режимы рынка сильно меняют результат
   при winrate 34–36%, который лежит ровно на границе безубытка с комиссией.

**ГЛАВНЫЙ ВЫВОД ВСЕЙ РАБОТЫ:** ни одна конфигурация (consensus, ADX-фильтры, режимная
адаптация, блок часов, отключение пресетов, гибридный вход, разные RR/ТФ) не показала
**устойчивый плюс на независимых (out-of-sample) данных**. Система на 1h с текущими
индикаторными сигналами фундаментально без edge: winrate 34–36% при RR 2:1 находится
на границе, которую решает комиссия, и эта граница колеблется от периода к периоду.

### 7.7 Рекомендуемое направление (обновлено)

- **Не доверять «найденным решениям» из одного периода.** Любой прирост winrate,
  полученный подгонкой (часы/пресеты/пороги), должен проходить out-of-sample проверку.
  Из всех экспериментов ни один её не прошёл.
- **Реальные фильтры (consensus=2, ADX≤25)** убирают основную массу убытка и могут
  быть включены как «меньшее зло», НО ожидать от них стабильной прибыли нельзя —
  они лишь снижают убыток, не создавая edge.
- Для выхода в плюс нужен **другой подход к сигналу** (не 46 индикаторных
  пересечений на закрытии 1h-свечи), либо winrate 40%+ на независимых периодах,
  либо существенное снижение комиссии/частоты сделок.
- Либо принять «около нуля / слабый минус» как предел текущей архитектуры и
  использовать систему только как исследовательский стенд, а не торговую.

### 7.8 ТЕКУЩИЙ ЭКСПЕРИМЕНТ: Inside Bar Breakout + RSI≥70 (forward test, с 2026-09-08)

**Контекст поиска.** После череды неудачных подходов (46 пресетов, consensus,
режимы, RR, гибридный вход) перешли к свечным паттернам. Inside Bar тестировался
в трёх вариантах: строгий (75-89 сделок — «казалось, работает», оказалось шумом),
ослабленный (4024 сделки — winrate 32-33%, стабильный минус), трейлинг-выход (хуже
фикс. в 2.5 раза). Вывод: паттерн сам по себе edge не даёт.

**Находка.** Постфактум-анализ 4024 сделок выявил срез **LONG + RSI≥70** на пробое
консолидации: winrate 37.5%, PnL +23.6 на июне-сентябре. Это контринтуитивно
(перекупленность = сила для breakout, не разворот). Однако:
- на consensus=2 входах правило НЕ работает (март-май даже убыточно) — не универсальный фильтр;
- на октябре-феврале 2026 (независимый период) даёт 34.4% WR, −4.0 — режим-зависимо.

**Решение:** включить правило как **paper-forward тест** на всех 35 ботах, чтобы
ответить на вопрос «работает ли в ТЕКУЩЕМ режиме» (история не может ответить на это —
рынок мог сменить режим). Наблюдение вперёд с заранее заданными критериями.

**Реализация (2026-09-08):**
- Новый сигнал `inside_bar_rsi_long` в `strategy.py::get_signal_inside_bar_rsi`:
  консолидация MB + 1-5 IB → пробой LONG по close → RSI≥70 → объём ≥1.5×volume_ma20.
  Уровни задаёт сам сигнал: SL = низ консолидации, TP = 2×SL (у пресета НЕТ ключа
  "tp", поэтому main.py не пересчитывает SL/TP по ATR).
- Учтено «первая свеча серии пробоя» (не дублировать сигнал несколько часов подряд).
- Пресет зарегистрирован в `preset_config.py` (mode: paper, без tp/sl) и `preset_funcs`.
- Во всех 35 конфигах активен ТОЛЬКО `inside_bar_rsi_long`; остальные 46
  закомментированы. `min_consensus` не задан (дефолт 1 — одна стратегия проходит).
- Все 35 ботов перезапущены 2026-09-08 ~08:47 UTC.

**Ожидание и критерий отключения:**
- Паттерн редкий: единицы сделок в день на все 35 монет.
- Сделки пишутся в БД как обычные paper (status=closed).
- **Критерий через 3-4 недели (~100+ сделок):** если winrate ≥ ~38% и PnL>0 — правило
  работает в текущем режиме, оставляем. Если ниже — отключаем, откатываем конфиги.

**Восстановление старой конфигурации** (если эксперимент не сработает): раскомментировать
все 46 пресетов в config_*.yaml (обратная операция к enable_insidebar).

---

## 8. Как смотреть статистику / запускать анализ

- **Живая статистика:** дашборд :5173, или напрямую SQLite:
  ```sql
  SELECT direction, COUNT(*), SUM(pnl) FROM trades WHERE status='closed' GROUP BY direction;
  SELECT preset, direction, COUNT(*), SUM(pnl) FROM trades WHERE status='closed' GROUP BY preset, direction;
  ```
- **Per-preset бэктест с режим-разбивкой (каждый пресет изолированно):**
  ```powershell
  cd bot
  python backtest_presets.py --start 2026-06-01 --end 2026-09-07
  ```
  CSV: строки `regime=all` + разбивка `trend`/`weak`/`flat` по ADX на входе.
- **Бэктест LONG vs SHORT с выбором RR, консенсуса и режима:**
  ```powershell
  python backtest_direction.py --start 2026-06-01 --end 2026-09-07                    # база (RR из конфигов)
  python backtest_direction.py --start ... --end ... --consensus 2 --max-adx 25       # ЛУЧШИЙ вариант
  python backtest_direction.py --start ... --end ... --consensus 2 --timeframe 15m    # другой ТФ
  python backtest_direction.py --start ... --end ... --tp-mult 2.2                    # изменение RR
  python backtest_direction.py --start ... --end ... --tp1-close-pct 60 --tp2-mult 3.0
  python backtest_direction.py --start ... --end ... --consensus-flat 1 --consensus-weak 2 --consensus-trend 3 --block-short-trend  # режимный consensus
  python backtest_direction.py --start ... --end ... --consensus 2 --max-adx 25 --block-hours 2,6,10,12,15,16 --exclude-presets stoch_bounce_long,macd_momentum_long  # COMBO (проверять OOS!)
  ```
  В консоли дополнительно печатаются «ТОП/ХУДШИЕ КОМБИНАЦИИ СТРАТЕГИЙ» (какие пары/
  группы стратегий дают консенсус и их результат). Каждая сделка хранит список
  согласовавших стратегий (`voting_bases`). По завершении сохраняются ДВА файла:
  `backtest_direction_<ts>.csv` (агрегаты по символам) и
  `backtest_trades_<ts>.csv` (одна строка = одна сделка: symbol, preset, direction,
  entry/exit price, entry/exit time, pnl, exit_reason, adx_at_entry, atr_pct,
  commission, entry_qty, sl_price, tp_price, atr_value, bases).
- **Гибридный бэктест (1h сигнал + M15 подтверждение входа):**
  ```powershell
  python backtest_hybrid.py --start 2026-06-01 --end 2026-09-07 --consensus 2 --confirm-m15 1
  ```
- **Анализ по часовым окнам / дням** — скрипты в `bot/*.py` + `analyze_trades.py`.

**Важные предостережения при анализе:**
1. Конфиги монет **разные** (EMA, volume_multiplier, leverage) — сравнивать монеты
   напрямую можно, но параметры сигналов у них не одинаковые.
2. В живом режиме пресеты **конкурируют** за один вход на свечу (выбор max volume),
   поэтому per-symbol результат ≠ сумме изолированных пресетов.
3. Размер сделки зависит от `fixed_risk_usd` и **реального ATR-SL сигнала**, а не от
   плеча и не от `sl_pct` из конфига (qty = fixed_risk_usd / (entry × SL%реальный)).
4. Изменение RR меняет время удержания позиций, а значит и последующие сигналы
   (path-dependent) — результаты разных RR нельзя складывать.
5. В бэктесте (после фикса) qty считается как в live (fixed_risk_usd от реального SL),
   комиссия commission_pct вычитается из каждой сделки, маржа не может превысить
   баланс. Ранние прогоны (до фикса) с risk_pct-компаундингом давали нереальные PnL
   (сотни тысяч) — их использовать нельзя, только winrate был валиден.
6. Сделки из per-preset изоляции помечаются режимом рынка на входе
   (`regime_adx`, `regime_atr_pct`, `regime_trend`) — при анализе CSV фильтровать
   `regime=all` как общий итог, остальные — разбивка по ADX.

---

## 9. Ключевые файлы для анализа кода

| Файл | Что смотреть |
|---|---|
| `bot/strategy.py` | Все 46 функций-пресетов, `get_all_signals` (consensus + voting_bases), `_calc_atr_sl_tp` |
| `bot/preset_config.py` | TP/SL/лимиты пресетов, fallback-логика |
| `bot/main.py` | Цикл свечей, режимный consensus, выбор сигнала, ATR-пересчёт, guard |
| `bot/backtester.py` | `run_backtest_on_df` — движок (честный sizing, regime-поля, voting_bases, режимный consensus) |
| `bot/config_<sym>.yaml` | Per-symbol параметры (разные для монет) |
| `bot/config.py` | `atr_tp_multiplier*`, `min_consensus`, `consensus_flat/weak/trend`, `trend_block_short`, `block_hours_utc`, `excluded_presets`, ADX-фильтры |
| `bot/backtest_direction.py` | Бэктест LONG/SHORT: `--consensus`, `--max-adx`, режимный consensus, `--block-hours`, `--exclude-presets`, анализ пар, per-trade CSV |
| `bot/backtest_insidebar.py` | Бэктест Inside Bar (строгий/ослабленный, fixed/trailing, `--periods`) |
| `bot/backtest_presets.py` | Изоляция пресетов + режим-разбивка (CSV) |
| `bot/symbol_preset_matrix.py` | Матрица монета×пресет×направление на 2 периодах (поиск стабильных связок) |
| `bot/strategy.py::get_signal_inside_bar_rsi` | Текущий live-сигнал: Inside Bar + RSI≥70 (forward test) |
| `bot/spec_inside_bar.md` | Спецификация Inside Bar паттерна |
